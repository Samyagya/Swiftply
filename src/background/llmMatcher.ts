/**
 * llmMatcher — Phase 6 stub.
 * Sends unmatched field labels + minimal profile subset to Anthropic API.
 * BYO API key (stored in chrome.storage.local). Off by default.
 * Zod-validates all LLM responses before use (rules.md §2).
 * Implemented in Phase 6. See Phases.md.
 */

// Placeholder export to satisfy TypeScript module requirements.
export {}
