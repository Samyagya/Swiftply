/**
 * FillButton — Phase 5 stub.
 * Triggers scan → match → fill pipeline. Shows fill summary and Undo button.
 * Implemented in Phase 5. See Phases.md.
 */
export default function FillButton(): null {
  return null
}
